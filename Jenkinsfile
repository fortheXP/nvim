pipeline {
  agent any
  stages {
    stage('Type File')
    {
      steps {
                script {
                  // reading a file
                  bash cat 'init.lua' 
                }
      }
    }
  }
}
