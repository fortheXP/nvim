pipeline {
  agent any
  stages {
    stage('Type File')
    {
      steps {
                script {
                  // reading a file
                  sh 'cat init.lua' 
                }
      }
    }
  }
}
